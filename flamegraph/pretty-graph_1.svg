<?xml version="1.0" standalone="no"?>
<!DOCTYPE svg PUBLIC "-//W3C//DTD SVG 1.1//EN" "http://www.w3.org/Graphics/SVG/1.1/DTD/svg11.dtd">
<svg version="1.1" width="1200" height="342" onload="init(evt)" viewBox="0 0 1200 342" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink">
<!-- Flame graph stack visualization. See https://github.com/brendangregg/FlameGraph for latest version, and http://www.brendangregg.com/flamegraphs.html for examples. -->
<!-- NOTES:  -->
<defs>
	<linearGradient id="background" y1="0" y2="1" x1="0" x2="0" >
		<stop stop-color="#eeeeee" offset="5%" />
		<stop stop-color="#eeeeb0" offset="95%" />
	</linearGradient>
</defs>
<style type="text/css">
	text { font-family:Verdana; font-size:12px; fill:rgb(0,0,0); }
	#search, #ignorecase { opacity:0.1; cursor:pointer; }
	#search:hover, #search.show, #ignorecase:hover, #ignorecase.show { opacity:1; }
	#subtitle { text-anchor:middle; font-color:rgb(160,160,160); }
	#title { text-anchor:middle; font-size:17px}
	#unzoom { cursor:pointer; }
	#frames > *:hover { stroke:black; stroke-width:0.5; cursor:pointer; }
	.hide { display:none; }
	.parent { opacity:0.5; }
</style>
<script type="text/ecmascript">
<![CDATA[
	"use strict";
	var details, searchbtn, unzoombtn, matchedtxt, svg, searching, currentSearchTerm, ignorecase, ignorecaseBtn;
	function init(evt) {
		details = document.getElementById("details").firstChild;
		searchbtn = document.getElementById("search");
		ignorecaseBtn = document.getElementById("ignorecase");
		unzoombtn = document.getElementById("unzoom");
		matchedtxt = document.getElementById("matched");
		svg = document.getElementsByTagName("svg")[0];
		searching = 0;
		currentSearchTerm = null;

		// use GET parameters to restore a flamegraphs state.
		var params = get_params();
		if (params.x && params.y)
			zoom(find_group(document.querySelector('[x="' + params.x + '"][y="' + params.y + '"]')));
                if (params.s) search(params.s);
	}

	// event listeners
	window.addEventListener("click", function(e) {
		var target = find_group(e.target);
		if (target) {
			if (target.nodeName == "a") {
				if (e.ctrlKey === false) return;
				e.preventDefault();
			}
			if (target.classList.contains("parent")) unzoom(true);
			zoom(target);
			if (!document.querySelector('.parent')) {
				// we have basically done a clearzoom so clear the url
				var params = get_params();
				if (params.x) delete params.x;
				if (params.y) delete params.y;
				history.replaceState(null, null, parse_params(params));
				unzoombtn.classList.add("hide");
				return;
			}

			// set parameters for zoom state
			var el = target.querySelector("rect");
			if (el && el.attributes && el.attributes.y && el.attributes._orig_x) {
				var params = get_params()
				params.x = el.attributes._orig_x.value;
				params.y = el.attributes.y.value;
				history.replaceState(null, null, parse_params(params));
			}
		}
		else if (e.target.id == "unzoom") clearzoom();
		else if (e.target.id == "search") search_prompt();
		else if (e.target.id == "ignorecase") toggle_ignorecase();
	}, false)

	// mouse-over for info
	// show
	window.addEventListener("mouseover", function(e) {
		var target = find_group(e.target);
		if (target) details.nodeValue = "Function: " + g_to_text(target);
	}, false)

	// clear
	window.addEventListener("mouseout", function(e) {
		var target = find_group(e.target);
		if (target) details.nodeValue = ' ';
	}, false)

	// ctrl-F for search
	// ctrl-I to toggle case-sensitive search
	window.addEventListener("keydown",function (e) {
		if (e.keyCode === 114 || (e.ctrlKey && e.keyCode === 70)) {
			e.preventDefault();
			search_prompt();
		}
		else if (e.ctrlKey && e.keyCode === 73) {
			e.preventDefault();
			toggle_ignorecase();
		}
	}, false)

	// functions
	function get_params() {
		var params = {};
		var paramsarr = window.location.search.substr(1).split('&');
		for (var i = 0; i < paramsarr.length; ++i) {
			var tmp = paramsarr[i].split("=");
			if (!tmp[0] || !tmp[1]) continue;
			params[tmp[0]]  = decodeURIComponent(tmp[1]);
		}
		return params;
	}
	function parse_params(params) {
		var uri = "?";
		for (var key in params) {
			uri += key + '=' + encodeURIComponent(params[key]) + '&';
		}
		if (uri.slice(-1) == "&")
			uri = uri.substring(0, uri.length - 1);
		if (uri == '?')
			uri = window.location.href.split('?')[0];
		return uri;
	}
	function find_child(node, selector) {
		var children = node.querySelectorAll(selector);
		if (children.length) return children[0];
	}
	function find_group(node) {
		var parent = node.parentElement;
		if (!parent) return;
		if (parent.id == "frames") return node;
		return find_group(parent);
	}
	function orig_save(e, attr, val) {
		if (e.attributes["_orig_" + attr] != undefined) return;
		if (e.attributes[attr] == undefined) return;
		if (val == undefined) val = e.attributes[attr].value;
		e.setAttribute("_orig_" + attr, val);
	}
	function orig_load(e, attr) {
		if (e.attributes["_orig_"+attr] == undefined) return;
		e.attributes[attr].value = e.attributes["_orig_" + attr].value;
		e.removeAttribute("_orig_"+attr);
	}
	function g_to_text(e) {
		var text = find_child(e, "title").firstChild.nodeValue;
		return (text)
	}
	function g_to_func(e) {
		var func = g_to_text(e);
		// if there's any manipulation we want to do to the function
		// name before it's searched, do it here before returning.
		return (func);
	}
	function update_text(e) {
		var r = find_child(e, "rect");
		var t = find_child(e, "text");
		var w = parseFloat(r.attributes.width.value) -3;
		var txt = find_child(e, "title").textContent.replace(/\([^(]*\)$/,"");
		t.attributes.x.value = parseFloat(r.attributes.x.value) + 3;

		// Smaller than this size won't fit anything
		if (w < 2 * 12 * 0.59) {
			t.textContent = "";
			return;
		}

		t.textContent = txt;
		var sl = t.getSubStringLength(0, txt.length);
		// check if only whitespace or if we can fit the entire string into width w
		if (/^ *$/.test(txt) || sl < w)
			return;

		// this isn't perfect, but gives a good starting point
		// and avoids calling getSubStringLength too often
		var start = Math.floor((w/sl) * txt.length);
		for (var x = start; x > 0; x = x-2) {
			if (t.getSubStringLength(0, x + 2) <= w) {
				t.textContent = txt.substring(0, x) + "..";
				return;
			}
		}
		t.textContent = "";
	}

	// zoom
	function zoom_reset(e) {
		if (e.attributes != undefined) {
			orig_load(e, "x");
			orig_load(e, "width");
		}
		if (e.childNodes == undefined) return;
		for (var i = 0, c = e.childNodes; i < c.length; i++) {
			zoom_reset(c[i]);
		}
	}
	function zoom_child(e, x, ratio) {
		if (e.attributes != undefined) {
			if (e.attributes.x != undefined) {
				orig_save(e, "x");
				e.attributes.x.value = (parseFloat(e.attributes.x.value) - x - 10) * ratio + 10;
				if (e.tagName == "text")
					e.attributes.x.value = find_child(e.parentNode, "rect[x]").attributes.x.value + 3;
			}
			if (e.attributes.width != undefined) {
				orig_save(e, "width");
				e.attributes.width.value = parseFloat(e.attributes.width.value) * ratio;
			}
		}

		if (e.childNodes == undefined) return;
		for (var i = 0, c = e.childNodes; i < c.length; i++) {
			zoom_child(c[i], x - 10, ratio);
		}
	}
	function zoom_parent(e) {
		if (e.attributes) {
			if (e.attributes.x != undefined) {
				orig_save(e, "x");
				e.attributes.x.value = 10;
			}
			if (e.attributes.width != undefined) {
				orig_save(e, "width");
				e.attributes.width.value = parseInt(svg.width.baseVal.value) - (10 * 2);
			}
		}
		if (e.childNodes == undefined) return;
		for (var i = 0, c = e.childNodes; i < c.length; i++) {
			zoom_parent(c[i]);
		}
	}
	function zoom(node) {
		var attr = find_child(node, "rect").attributes;
		var width = parseFloat(attr.width.value);
		var xmin = parseFloat(attr.x.value);
		var xmax = parseFloat(xmin + width);
		var ymin = parseFloat(attr.y.value);
		var ratio = (svg.width.baseVal.value - 2 * 10) / width;

		// XXX: Workaround for JavaScript float issues (fix me)
		var fudge = 0.0001;

		unzoombtn.classList.remove("hide");

		var el = document.getElementById("frames").children;
		for (var i = 0; i < el.length; i++) {
			var e = el[i];
			var a = find_child(e, "rect").attributes;
			var ex = parseFloat(a.x.value);
			var ew = parseFloat(a.width.value);
			var upstack;
			// Is it an ancestor
			if (0 == 0) {
				upstack = parseFloat(a.y.value) > ymin;
			} else {
				upstack = parseFloat(a.y.value) < ymin;
			}
			if (upstack) {
				// Direct ancestor
				if (ex <= xmin && (ex+ew+fudge) >= xmax) {
					e.classList.add("parent");
					zoom_parent(e);
					update_text(e);
				}
				// not in current path
				else
					e.classList.add("hide");
			}
			// Children maybe
			else {
				// no common path
				if (ex < xmin || ex + fudge >= xmax) {
					e.classList.add("hide");
				}
				else {
					zoom_child(e, xmin, ratio);
					update_text(e);
				}
			}
		}
		search();
	}
	function unzoom(dont_update_text) {
		unzoombtn.classList.add("hide");
		var el = document.getElementById("frames").children;
		for(var i = 0; i < el.length; i++) {
			el[i].classList.remove("parent");
			el[i].classList.remove("hide");
			zoom_reset(el[i]);
			if(!dont_update_text) update_text(el[i]);
		}
		search();
	}
	function clearzoom() {
		unzoom();

		// remove zoom state
		var params = get_params();
		if (params.x) delete params.x;
		if (params.y) delete params.y;
		history.replaceState(null, null, parse_params(params));
	}

	// search
	function toggle_ignorecase() {
		ignorecase = !ignorecase;
		if (ignorecase) {
			ignorecaseBtn.classList.add("show");
		} else {
			ignorecaseBtn.classList.remove("show");
		}
		reset_search();
		search();
	}
	function reset_search() {
		var el = document.querySelectorAll("#frames rect");
		for (var i = 0; i < el.length; i++) {
			orig_load(el[i], "fill")
		}
		var params = get_params();
		delete params.s;
		history.replaceState(null, null, parse_params(params));
	}
	function search_prompt() {
		if (!searching) {
			var term = prompt("Enter a search term (regexp " +
			    "allowed, eg: ^ext4_)"
			    + (ignorecase ? ", ignoring case" : "")
			    + "\nPress Ctrl-i to toggle case sensitivity", "");
			if (term != null) search(term);
		} else {
			reset_search();
			searching = 0;
			currentSearchTerm = null;
			searchbtn.classList.remove("show");
			searchbtn.firstChild.nodeValue = "Search"
			matchedtxt.classList.add("hide");
			matchedtxt.firstChild.nodeValue = ""
		}
	}
	function search(term) {
		if (term) currentSearchTerm = term;
		if (currentSearchTerm === null) return;

		var re = new RegExp(currentSearchTerm, ignorecase ? 'i' : '');
		var el = document.getElementById("frames").children;
		var matches = new Object();
		var maxwidth = 0;
		for (var i = 0; i < el.length; i++) {
			var e = el[i];
			var func = g_to_func(e);
			var rect = find_child(e, "rect");
			if (func == null || rect == null)
				continue;

			// Save max width. Only works as we have a root frame
			var w = parseFloat(rect.attributes.width.value);
			if (w > maxwidth)
				maxwidth = w;

			if (func.match(re)) {
				// highlight
				var x = parseFloat(rect.attributes.x.value);
				orig_save(rect, "fill");
				rect.attributes.fill.value = "rgb(230,0,230)";

				// remember matches
				if (matches[x] == undefined) {
					matches[x] = w;
				} else {
					if (w > matches[x]) {
						// overwrite with parent
						matches[x] = w;
					}
				}
				searching = 1;
			}
		}
		if (!searching)
			return;
		var params = get_params();
		params.s = currentSearchTerm;
		history.replaceState(null, null, parse_params(params));

		searchbtn.classList.add("show");
		searchbtn.firstChild.nodeValue = "Reset Search";

		// calculate percent matched, excluding vertical overlap
		var count = 0;
		var lastx = -1;
		var lastw = 0;
		var keys = Array();
		for (k in matches) {
			if (matches.hasOwnProperty(k))
				keys.push(k);
		}
		// sort the matched frames by their x location
		// ascending, then width descending
		keys.sort(function(a, b){
			return a - b;
		});
		// Step through frames saving only the biggest bottom-up frames
		// thanks to the sort order. This relies on the tree property
		// where children are always smaller than their parents.
		var fudge = 0.0001;	// JavaScript floating point
		for (var k in keys) {
			var x = parseFloat(keys[k]);
			var w = matches[keys[k]];
			if (x >= lastx + lastw - fudge) {
				count += w;
				lastx = x;
				lastw = w;
			}
		}
		// display matched percent
		matchedtxt.classList.remove("hide");
		var pct = 100 * count / maxwidth;
		if (pct != 100) pct = pct.toFixed(1)
		matchedtxt.firstChild.nodeValue = "Matched: " + pct + "%";
	}
]]>
</script>
<rect x="0.0" y="0" width="1200.0" height="342.0" fill="url(#background)"  />
<text id="title" x="600.00" y="24" >Flame Graph</text>
<text id="details" x="10.00" y="325" > </text>
<text id="unzoom" x="10.00" y="24" class="hide">Reset Zoom</text>
<text id="search" x="1090.00" y="24" >Search</text>
<text id="ignorecase" x="1174.00" y="24" >ic</text>
<text id="matched" x="1090.00" y="325" > </text>
<g id="frames">
<g >
<title>libsystem_malloc.dylib`_realloc (4 samples, 0.31%)</title><rect x="554.5" y="85" width="3.6" height="15.0" fill="rgb(249,206,49)" rx="2" ry="2" />
<text  x="557.48" y="95.5" ></text>
</g>
<g >
<title>ddnn`anyhow::error::_$LT$impl$u20$anyhow..Error$GT$::construct::h368a89c38d31bf10 (20 samples, 1.54%)</title><rect x="559.9" y="181" width="18.2" height="15.0" fill="rgb(248,198,47)" rx="2" ry="2" />
<text  x="562.94" y="191.5" ></text>
</g>
<g >
<title>ddnn`anyhow::error::_$LT$impl$u20$anyhow..Error$GT$::msg::h2a399bfb0c35dbbc (12 samples, 0.93%)</title><rect x="65.5" y="197" width="11.0" height="15.0" fill="rgb(250,209,50)" rx="2" ry="2" />
<text  x="68.54" y="207.5" ></text>
</g>
<g >
<title>ddnn`DYLD-STUB$$malloc (2 samples, 0.15%)</title><rect x="578.1" y="165" width="1.9" height="15.0" fill="rgb(243,175,41)" rx="2" ry="2" />
<text  x="581.15" y="175.5" ></text>
</g>
<g >
<title>ddnn`core::fmt::write::h164e2e78980a3344 (6 samples, 0.46%)</title><rect x="554.5" y="165" width="5.4" height="15.0" fill="rgb(223,86,20)" rx="2" ry="2" />
<text  x="557.48" y="175.5" ></text>
</g>
<g >
<title>ddnn`std::rt::lang_start_internal::h1e9a2dfabf97dcbe (1,295 samples, 99.92%)</title><rect x="10.0" y="245" width="1179.1" height="15.0" fill="rgb(237,148,35)" rx="2" ry="2" />
<text  x="13.00" y="255.5" >ddnn`std::rt::lang_start_internal::h1e9a2dfabf97dcbe</text>
</g>
<g >
<title>libsystem_malloc.dylib`_free (62 samples, 4.78%)</title><rect x="1052.5" y="197" width="56.5" height="15.0" fill="rgb(216,54,13)" rx="2" ry="2" />
<text  x="1055.52" y="207.5" >libsy..</text>
</g>
<g >
<title>ddnn`_$LT$str$u20$as$u20$core..fmt..Debug$GT$::fmt::h78d738a10a78752b (2 samples, 0.15%)</title><rect x="558.1" y="133" width="1.8" height="15.0" fill="rgb(209,22,5)" rx="2" ry="2" />
<text  x="561.12" y="143.5" ></text>
</g>
<g >
<title>ddnn`ddnn::main::hf82941fbff14a7d3 (976 samples, 75.31%)</title><rect x="78.3" y="197" width="888.6" height="15.0" fill="rgb(243,175,41)" rx="2" ry="2" />
<text  x="81.29" y="207.5" >ddnn`ddnn::main::hf82941fbff14a7d3</text>
</g>
<g >
<title>ddnn`alloc::raw_vec::RawVecInner$LT$A$GT$::finish_grow::h5779540bd9d0a3cd (4 samples, 0.31%)</title><rect x="554.5" y="101" width="3.6" height="15.0" fill="rgb(247,193,46)" rx="2" ry="2" />
<text  x="557.48" y="111.5" ></text>
</g>
<g >
<title>ddnn`std::rt::lang_start::_$u7b$$u7b$closure$u7d$$u7d$::h0bb7137a8d5edc04 (1,295 samples, 99.92%)</title><rect x="10.0" y="229" width="1179.1" height="15.0" fill="rgb(206,7,1)" rx="2" ry="2" />
<text  x="13.00" y="239.5" >ddnn`std::rt::lang_start::_$u7b$$u7b$closure$u7d$$u7d$::h0bb7137a8d5edc04</text>
</g>
<g >
<title>ddnn`std::sys::backtrace::__rust_begin_short_backtrace::hbaada41a18263432 (1,295 samples, 99.92%)</title><rect x="10.0" y="213" width="1179.1" height="15.0" fill="rgb(249,205,49)" rx="2" ry="2" />
<text  x="13.00" y="223.5" >ddnn`std::sys::backtrace::__rust_begin_short_backtrace::hbaada41a18263432</text>
</g>
<g >
<title>ddnn`_$LT$alloc..string..String$u20$as$u20$core..fmt..Write$GT$::write_str::h07f07e45209a5724 (4 samples, 0.31%)</title><rect x="554.5" y="133" width="3.6" height="15.0" fill="rgb(219,68,16)" rx="2" ry="2" />
<text  x="557.48" y="143.5" ></text>
</g>
<g >
<title>libsystem_malloc.dylib`_xzm_free (126 samples, 9.72%)</title><rect x="814.9" y="181" width="114.7" height="15.0" fill="rgb(235,141,33)" rx="2" ry="2" />
<text  x="817.88" y="191.5" >libsystem_mall..</text>
</g>
<g >
<title>ddnn`anyhow::error::object_drop::h642a7318584ed6c3 (2 samples, 0.15%)</title><rect x="755.7" y="181" width="1.8" height="15.0" fill="rgb(238,154,36)" rx="2" ry="2" />
<text  x="758.69" y="191.5" ></text>
</g>
<g >
<title>ddnn`alloc::fmt::format::format_inner::hfba7744daf75a2c1 (7 samples, 0.54%)</title><rect x="553.6" y="181" width="6.3" height="15.0" fill="rgb(247,194,46)" rx="2" ry="2" />
<text  x="556.56" y="191.5" ></text>
</g>
<g >
<title>libsystem_kernel.dylib`__exit (1 samples, 0.08%)</title><rect x="1189.1" y="277" width="0.9" height="15.0" fill="rgb(240,161,38)" rx="2" ry="2" />
<text  x="1192.09" y="287.5" ></text>
</g>
<g >
<title>libsystem_platform.dylib`__bzero (36 samples, 2.78%)</title><rect x="934.2" y="181" width="32.7" height="15.0" fill="rgb(245,185,44)" rx="2" ry="2" />
<text  x="937.15" y="191.5" >li..</text>
</g>
<g >
<title>libsystem_malloc.dylib`DYLD-STUB$$_platform_bzero (16 samples, 1.23%)</title><rect x="800.3" y="181" width="14.6" height="15.0" fill="rgb(208,15,3)" rx="2" ry="2" />
<text  x="803.31" y="191.5" ></text>
</g>
<g >
<title>ddnn`DYLD-STUB$$free (13 samples, 1.00%)</title><rect x="10.0" y="197" width="11.8" height="15.0" fill="rgb(225,94,22)" rx="2" ry="2" />
<text  x="13.00" y="207.5" ></text>
</g>
<g >
<title>libsystem_platform.dylib`_platform_memmove (64 samples, 4.94%)</title><rect x="1130.8" y="197" width="58.3" height="15.0" fill="rgb(212,33,8)" rx="2" ry="2" />
<text  x="1133.82" y="207.5" >libsys..</text>
</g>
<g >
<title>ddnn`core::ptr::drop_in_place$LT$core..option..Option$LT$std..backtrace..Backtrace$GT$$GT$::h72dd04612220fcfa (22 samples, 1.70%)</title><rect x="757.5" y="181" width="20.0" height="15.0" fill="rgb(223,83,20)" rx="2" ry="2" />
<text  x="760.52" y="191.5" ></text>
</g>
<g >
<title>libsystem_malloc.dylib`_xzm_xzone_malloc (45 samples, 3.47%)</title><rect x="713.8" y="165" width="41.0" height="15.0" fill="rgb(246,190,45)" rx="2" ry="2" />
<text  x="716.81" y="175.5" >lib..</text>
</g>
<g >
<title>libsystem_malloc.dylib`xzm_realloc (4 samples, 0.31%)</title><rect x="554.5" y="53" width="3.6" height="15.0" fill="rgb(232,125,30)" rx="2" ry="2" />
<text  x="557.48" y="63.5" ></text>
</g>
<g >
<title>libsystem_kernel.dylib`read (94 samples, 7.25%)</title><rect x="966.9" y="197" width="85.6" height="15.0" fill="rgb(211,28,6)" rx="2" ry="2" />
<text  x="969.93" y="207.5" >libsystem_..</text>
</g>
<g >
<title>ddnn`std::backtrace::Backtrace::capture::h254f474da8c09fba (13 samples, 1.00%)</title><rect x="608.2" y="165" width="11.8" height="15.0" fill="rgb(248,198,47)" rx="2" ry="2" />
<text  x="611.19" y="175.5" ></text>
</g>
<g >
<title>ddnn`core::str::converts::from_utf8::h907df036b51c9b19 (2 samples, 0.15%)</title><rect x="76.5" y="197" width="1.8" height="15.0" fill="rgb(225,96,23)" rx="2" ry="2" />
<text  x="79.47" y="207.5" ></text>
</g>
<g >
<title>libsystem_malloc.dylib`&lt;deduplicated_symbol&gt; (2 samples, 0.15%)</title><rect x="798.5" y="181" width="1.8" height="15.0" fill="rgb(239,156,37)" rx="2" ry="2" />
<text  x="801.49" y="191.5" ></text>
</g>
<g >
<title>ddnn`main (1,295 samples, 99.92%)</title><rect x="10.0" y="261" width="1179.1" height="15.0" fill="rgb(210,25,6)" rx="2" ry="2" />
<text  x="13.00" y="271.5" >ddnn`main</text>
</g>
<g >
<title>all (1,296 samples, 100%)</title><rect x="10.0" y="293" width="1180.0" height="15.0" fill="rgb(213,39,9)" rx="2" ry="2" />
<text  x="13.00" y="303.5" ></text>
</g>
<g >
<title>libsystem_malloc.dylib`_malloc_zone_malloc (63 samples, 4.86%)</title><rect x="656.5" y="165" width="57.3" height="15.0" fill="rgb(206,6,1)" rx="2" ry="2" />
<text  x="659.45" y="175.5" >libsys..</text>
</g>
<g >
<title>ddnn`anyhow::error::_$LT$impl$u20$anyhow..Error$GT$::msg::hcb32ec04150ebaea (1 samples, 0.08%)</title><rect x="754.8" y="181" width="0.9" height="15.0" fill="rgb(241,165,39)" rx="2" ry="2" />
<text  x="757.78" y="191.5" ></text>
</g>
<g >
<title>libsystem_malloc.dylib`&lt;deduplicated_symbol&gt; (40 samples, 3.09%)</title><rect x="620.0" y="165" width="36.5" height="15.0" fill="rgb(239,156,37)" rx="2" ry="2" />
<text  x="623.03" y="175.5" >lib..</text>
</g>
<g >
<title>libsystem_platform.dylib`_platform_memset (2 samples, 0.15%)</title><rect x="556.3" y="37" width="1.8" height="15.0" fill="rgb(253,223,53)" rx="2" ry="2" />
<text  x="559.30" y="47.5" ></text>
</g>
<g >
<title>libsystem_malloc.dylib`_malloc_zone_realloc (4 samples, 0.31%)</title><rect x="554.5" y="69" width="3.6" height="15.0" fill="rgb(221,76,18)" rx="2" ry="2" />
<text  x="557.48" y="79.5" ></text>
</g>
<g >
<title>dyld`start (1,295 samples, 99.92%)</title><rect x="10.0" y="277" width="1179.1" height="15.0" fill="rgb(217,57,13)" rx="2" ry="2" />
<text  x="13.00" y="287.5" >dyld`start</text>
</g>
<g >
<title>libsystem_malloc.dylib`_xzm_xzone_malloc (5 samples, 0.39%)</title><rect x="929.6" y="181" width="4.6" height="15.0" fill="rgb(246,190,45)" rx="2" ry="2" />
<text  x="932.60" y="191.5" ></text>
</g>
<g >
<title>ddnn`anyhow::__private::format_err::heaa82fb452894274 (41 samples, 3.16%)</title><rect x="28.2" y="197" width="37.3" height="15.0" fill="rgb(232,126,30)" rx="2" ry="2" />
<text  x="31.21" y="207.5" >ddn..</text>
</g>
<g >
<title>ddnn`std::backtrace::Backtrace::capture::h254f474da8c09fba (23 samples, 1.77%)</title><rect x="777.5" y="181" width="21.0" height="15.0" fill="rgb(248,198,47)" rx="2" ry="2" />
<text  x="780.55" y="191.5" ></text>
</g>
<g >
<title>libsystem_malloc.dylib`_xzm_free (24 samples, 1.85%)</title><rect x="1109.0" y="197" width="21.8" height="15.0" fill="rgb(235,141,33)" rx="2" ry="2" />
<text  x="1111.97" y="207.5" >l..</text>
</g>
<g >
<title>ddnn`anyhow::error::_$LT$impl$u20$anyhow..Error$GT$::construct::h368a89c38d31bf10 (31 samples, 2.39%)</title><rect x="580.0" y="165" width="28.2" height="15.0" fill="rgb(248,198,47)" rx="2" ry="2" />
<text  x="582.97" y="175.5" >d..</text>
</g>
<g >
<title>libsystem_malloc.dylib`_xzm_free (1 samples, 0.08%)</title><rect x="555.4" y="37" width="0.9" height="15.0" fill="rgb(235,141,33)" rx="2" ry="2" />
<text  x="558.39" y="47.5" ></text>
</g>
<g >
<title>ddnn`alloc::raw_vec::RawVecInner$LT$A$GT$::reserve::do_reserve_and_handle::h076dffaa2fb6fbdc (4 samples, 0.31%)</title><rect x="554.5" y="117" width="3.6" height="15.0" fill="rgb(208,16,4)" rx="2" ry="2" />
<text  x="557.48" y="127.5" ></text>
</g>
<g >
<title>libsystem_malloc.dylib`_xzm_free (2 samples, 0.15%)</title><rect x="755.7" y="165" width="1.8" height="15.0" fill="rgb(235,141,33)" rx="2" ry="2" />
<text  x="758.69" y="175.5" ></text>
</g>
<g >
<title>ddnn`_$LT$core..result..Result$LT$T$C$E$GT$$u20$as$u20$core..fmt..Debug$GT$::fmt::h72325eba25964cd4 (6 samples, 0.46%)</title><rect x="554.5" y="149" width="5.4" height="15.0" fill="rgb(225,95,22)" rx="2" ry="2" />
<text  x="557.48" y="159.5" ></text>
</g>
<g >
<title>ddnn`anyhow::error::_$LT$impl$u20$anyhow..Error$GT$::construct::hd7f7a68b76ef4bf8 (1 samples, 0.08%)</title><rect x="754.8" y="165" width="0.9" height="15.0" fill="rgb(245,185,44)" rx="2" ry="2" />
<text  x="757.78" y="175.5" ></text>
</g>
<g >
<title>ddnn`DYLD-STUB$$memcpy (7 samples, 0.54%)</title><rect x="21.8" y="197" width="6.4" height="15.0" fill="rgb(220,70,16)" rx="2" ry="2" />
<text  x="24.84" y="207.5" ></text>
</g>
<g >
<title>ddnn`anyhow::error::_$LT$impl$u20$anyhow..Error$GT$::msg::h2a399bfb0c35dbbc (194 samples, 14.97%)</title><rect x="578.1" y="181" width="176.7" height="15.0" fill="rgb(250,209,50)" rx="2" ry="2" />
<text  x="581.15" y="191.5" >ddnn`anyhow::error::_$..</text>
</g>
</g>
</svg>
